# Using range() to Make a List of Numbers
even_numbers = list(range(2, 12, 2))  # The range() function has 3 inputs range(start, stop, step)
print(even_numbers)  # In this case by using STEP = 2, we get the EVEN numbers from 2 to 12

